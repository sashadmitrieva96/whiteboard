List<List<Num>> perms = (Num size):
  print(size)
  if size == 0:
    return List()
  else if size == 1:
    return List(List(1))

  List<List<Num>> prev = perms(size - 1)
  List<List<Num>> res = List()
  for p in prev:
    List<Num> q = prev.get(p)
    List<Num> r = range(0, size )
    for i in r:
      i = r.get(i)
      List<Num> temp = q.insert(i, size)
      res.push(temp)
  return res

List<List<Num>> result = perms(4)
print(result.length())
print(result)
