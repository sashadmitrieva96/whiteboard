// NOT DONE

class IfStatement {
  constructor(ifExp, ifBlock, eiExps, eiBlocks, eExp, eBlock) {
    this.ifExp = ifExp;
    this.ifBlock = ifBlock;
    this.eiExps = eiExps;
    this.eiBlocks = eiBlocks;
    this.eExp = eBlock;
  }

  toString() {
    return str = '( If )';
  }
}
